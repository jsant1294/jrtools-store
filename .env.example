DATABASE_URL=            # Neon Postgres connection string
BLOB_READ_WRITE_TOKEN=   # Vercel Blob
ADMIN_PIN=               # non-techie admin login (big keypad UI)
NEXT_PUBLIC_STORE_PHONE= # WhatsApp / call number
PAYMENTS_STRIPE_ENABLED=false   # flip to true when ready — no code change
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PAYMENTS_PAYPAL_ENABLED=false   # server-side PayPal capture switch
NEXT_PUBLIC_PAYMENTS_PAYPAL_ENABLED=false # storefront PayPal button switch
PAYPAL_ENV=sandbox             # sandbox | live
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
ASSISTANT_AI_ENABLED=false     # keep false for local FAQ/catalog widget
ANTHROPIC_API_KEY=       # EMPTY for launch. Widget runs on FAQ retrieval.
                         # Set this key = AI salesman turns on. One env var.
